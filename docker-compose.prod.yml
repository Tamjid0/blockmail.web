# ============================================
# BLOCKMAIL SAAS — PRODUCTION DOCKER COMPOSE
# ============================================
# Usage: docker compose -f docker-compose.prod.yml up -d
# Adds: Logging, resource limits, health checks
# ============================================

services:
  db:
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 512M

  redis:
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 128M

  auth:
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 64M

  rest:
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 64M

  kong:
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 64M

  meta:
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 64M

  studio:
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 128M

  blockmail-engine:
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 128M

  web:
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 128M
